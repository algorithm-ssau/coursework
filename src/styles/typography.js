import { css } from 'styled-components/macro';

const title = css`
  font-size: 24px;
  line-height: 24px;
  font-weight: 600;
`;

const text = css`
  font-size: 16px;
  line-height: 24px;
  font-weight: 400;
`;

const label = css`
  font-size: 16px;
  line-height: 16px;
  font-weight: 400;
`;

export default {
  title,
  text,
  label
};
